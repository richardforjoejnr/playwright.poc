const BasePage = require("./../base.page");

class WelcomePage extends BasePage {
  constructor(page) {
    const path = "world";
    super(page, path);

    // locators
  }
}

module.exports = WelcomePage